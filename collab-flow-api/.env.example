PORT=3002
NODE_ENV=development
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# A strong, random string used to sign internal JWTs (must match the gateway's secret).
# Generate a secure secret with: node -p "require('crypto').randomBytes(64).toString('hex')"
JWT_SECRET=your-super-secret-and-long-random-string
JWT_EXPIRES_IN=15m

