{
  "name": "collab-flow",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky || true",
    "env:sync": "ts-node config/sync.ts",
    "test:config": "vitest run --dir config",
    "check:all": "npx tsc --noEmit && npm run test:config && npm --prefix ./sync-forge run test-lint-typecheck && npm --prefix ./google-oauth-backend run test-lint-typecheck && npm --prefix ./collab-flow-api run test-lint-typecheck"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.12",
    "husky": "^9.1.7",
    "ts-node": "^10.9.2",
    "vitest": "^1.6.0"
  }
}
